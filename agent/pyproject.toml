[project]
name = "agenthub-agent"
version = "0.2.0"
description = "AgentHub v0.2 Claude Agent SDK adapter"
requires-python = ">=3.12"
dependencies = ["claude-agent-sdk"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
