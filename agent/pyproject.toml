[project]
name = "agenthub-agent"
version = "0.1.0"
description = "AgentHub v0.1 Claude CLI adapter"
requires-python = ">=3.12"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
